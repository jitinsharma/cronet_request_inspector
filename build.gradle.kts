// Root build file. Nothing needed here -- module builds configure themselves. See
// proto/build.gradle.kts's comment for the Maven Central publishing setup and why
// it's gated behind a Gradle property instead of applied unconditionally.
